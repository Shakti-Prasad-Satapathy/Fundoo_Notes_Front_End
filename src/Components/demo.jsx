// import React, { Fragment } from 'react';
// import { JssProvider, jss } from 'react-jss';
// import {withStyles,createGenerateClassName} from '@material-ui/styles';
// import {createMuiTheme,MuiThemeProvider} from '@material-ui/core/styles';
// import Button from '@material-ui/core/Button';
// const styles = theme => ({root: {margin: theme.spacing(2)},
// contained: {extend: 'root',paddingTop: theme.spacing(2),paddingBottom: theme.spacing(2)},
// containedPrimary: {extend: 'contained',paddingLeft: theme.spacing(4),
// paddingRight: theme.spacing(4)}});
// const App = ({ children }) => ({children});
// const Buttons = withStyles(styles)(({ classes }) => (My Default ButtonMy Contained ButtonMy Contained Primary Button));const ExtendingComponentStyles = () => ();export default ExtendingComponentStyles;
